#include "utilisateur.h"

Utilisateur::Utilisateur(QString pseudo, QString nom, QString prenom, int age, int pdp, int idUtilisateur) :
    pseudo(pseudo), nom(nom), prenom(prenom), age(age), pdp(pdp), idUtilisateur(idUtilisateur)
{

}

